#include <stdio.h>
#include <stdint.h>
#include <assert.h>
#include "thing_obj.h"

void init_thing_obj_data(THING_OBJ *thing)
{
	assert(thing);
}
int resume_thing_obj_data(THING_OBJ *thing)
{
	assert(thing);
	return (0);
}	
